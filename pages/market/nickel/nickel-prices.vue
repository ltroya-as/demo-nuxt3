<template>
  <ProductPricePage
    :product="product"
    :product-data="productData"
    :subscription="subscription"
  />
</template>

<script>
import SeoUtilsMixin from '~/mixins/SeoUtilsMixin'

import { NICKEL, MARKET } from '~/constants/services/constants'
import nickelData from '~/constants/price-assessments/unauthorized/nickel'
import { PRICE_ASSESSMENTS_NICKEL } from '~/constants/services/price-assessments'

import ProductPricePage from '~/components/market/ProductPricePage.vue'

definePageMeta({ layout: 'services' })

export default {
  components: {
    ProductPricePage,
  },
  mixins: [SeoUtilsMixin],
  data() {
    return {
      product: NICKEL,
      productData: nickelData,
      subscription: PRICE_ASSESSMENTS_NICKEL.subscription,
    }
  },
  head() {
    return this.generateSeoInfo({
      title: `${MARKET.name}: ${this.product.name} Prices`,
    })
  },
}
</script>
